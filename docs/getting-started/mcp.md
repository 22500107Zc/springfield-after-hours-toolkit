# Local MCP server

Build first with `npm run build`, then register the stdio command used by your MCP client:

```sh
npm run mcp
```

Initial tools search the registry, list objective capability status, and validate in-memory campaign data. They do not expose environment variables or write files. Consult your MCP client's current official documentation for its project configuration format.

campaign:
  id: minimal-campaign
  title: Minimal Compiler Fixture
  version: 0.1.0
  description: Proves schema, registry, validator, and deterministic compiler flow.
missions:
  - id: fixture-mission
    player: fixture-player
    stages:
      - id: fixture-stage
        objective:
          type: noop
          note: This is not claimed to be playable.
dialogue: []

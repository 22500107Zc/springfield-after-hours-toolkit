version: 1
registry: vehicles
description: >-
  Vehicles, by internal name. Nothing is recorded yet: no source read during the
  foundation work stated the internal name of any vehicle, including the Honor
  Roller. Guessing a vehicle code would produce a mod that silently fails to
  load a car, so the registry stays empty until a source is found.

# INTENTIONALLY EMPTY.
#
# An empty registry is an honest statement: this toolkit has not verified any
# record of this kind from a citable source yet. It is NOT an invitation to
# add plausible-sounding entries.
#
# To add a record, see docs/registries/README.md. Every record needs a
# provenance source id that exists in data/provenance/sources.yaml.
records: []
